// Auto-generated: inline index.html for Cloudflare Workers
export const INDEX_HTML = `<!DOCTYPE html>
<html lang="en">
<head>
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-95PY8PSZ0Y"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-95PY8PSZ0Y');
</script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="title" content="FlowMachinery — Precision Industrial Machinery & Automation Solutions">
    <meta name="description" content="FlowMachinery delivers high-performance CNC machines, hydraulic systems, industrial automation, and manufacturing equipment. Trusted by 2,000+ manufacturers worldwide since 1998.">
    <meta name="keywords" content="industrial machinery, CNC machines, hydraulic systems, industrial automation, manufacturing equipment, precision engineering">
    <meta name="robots" content="index, follow">
    <meta name="msvalidate.01" content="C396E9907374E29FB46754412E4E3FB7" />
    <link rel="canonical" href="https://flowmachinery.com/">

    <!-- Open Graph -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://flowmachinery.com/">
    <meta property="og:title" content="FlowMachinery — Precision Industrial Machinery & Automation Solutions">
    <meta property="og:description" content="High-performance CNC machines, hydraulic systems, industrial automation, and manufacturing equipment. Trusted by 2,000+ manufacturers worldwide.">
    <meta property="og:image" content="https://flowmachinery.com/assets/og-image.jpg">
    <meta property="og:site_name" content="FlowMachinery">
    <meta property="og:locale" content="en_US">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="FlowMachinery — Precision Industrial Machinery & Automation Solutions">
    <meta name="twitter:description" content="High-performance CNC machines, hydraulic systems, industrial automation, and manufacturing equipment.">
    <meta name="twitter:image" content="https://flowmachinery.com/assets/og-image.jpg">

    <title>FlowMachinery — Precision Industrial Machinery & Automation Solutions</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "FlowMachinery",
        "url": "https://flowmachinery.com",
        "logo": "https://flowmachinery.com/assets/logo.svg",
        "description": "FlowMachinery delivers high-performance CNC machines, hydraulic systems, industrial automation, and manufacturing equipment for global manufacturers.",
        "foundingDate": "1998",
        "numberOfEmployees": {
            "@type": "QuantitativeValue",
            "minValue": 500,
            "maxValue": 1000
        },
        "address": {
            "@type": "PostalAddress",
            "streetAddress": "2800 Industrial Parkway, Suite 400",
            "addressLocality": "Cleveland",
            "addressRegion": "OH",
            "postalCode": "44115",
            "addressCountry": "US"
        },
        "contactPoint": {
            "@type": "ContactPoint",
            "telephone": "+1-800-555-0198",
            "contactType": "sales",
            "availableLanguage": ["English", "Spanish", "German", "Chinese"]
        },
        "sameAs": [
            "https://www.linkedin.com/company/flowmachinery",
            "https://www.youtube.com/@flowmachinery",
            "https://twitter.com/flowmachinery"
        ]
    }
    </script>

    <style>
        /* ===== RESET & BASE ===== */
        *, *::before, *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
            overflow-x: hidden;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            color: #e0e6ed;
            background-color: #0a0e17;
            line-height: 1.6;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        img {
            max-width: 100%;
            height: auto;
            display: block;
        }

        a {
            text-decoration: none;
            color: inherit;
            transition: color 0.3s ease;
        }

        ul {
            list-style: none;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Space Grotesk', 'Inter', sans-serif;
            line-height: 1.2;
            font-weight: 700;
        }

        .container {
            max-width: 1240px;
            margin: 0 auto;
            padding: 0 24px;
        }

        /* ===== ANIMATIONS ===== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-60px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(60px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes pulseGlow {
            0%, 100% { box-shadow: 0 0 20px rgba(59, 130, 246, 0.3); }
            50% { box-shadow: 0 0 40px rgba(59, 130, 246, 0.6); }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-12px); }
        }

        @keyframes countUp {
            from { opacity: 0; transform: scale(0.5); }
            to { opacity: 1; transform: scale(1); }
        }

        @keyframes gridMove {
            0% { background-position: 0 0; }
            100% { background-position: 60px 60px; }
        }

        .animate-on-scroll {
            opacity: 0;
            transform: translateY(40px);
            transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .animate-on-scroll.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .delay-1 { transition-delay: 0.1s; }
        .delay-2 { transition-delay: 0.2s; }
        .delay-3 { transition-delay: 0.3s; }
        .delay-4 { transition-delay: 0.4s; }

        /* ===== NAVIGATION ===== */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 0 24px;
            transition: all 0.4s ease;
            background: rgba(10, 14, 23, 0.6);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(59, 130, 246, 0.08);
        }

        .navbar.scrolled {
            background: rgba(10, 14, 23, 0.95);
            border-bottom: 1px solid rgba(59, 130, 246, 0.15);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.4);
        }

        .navbar .container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 72px;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.5rem;
            font-weight: 700;
            color: #ffffff;
            min-height: 44px;
            min-width: 44px;
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            background: linear-gradient(135deg, #3b82f6, #06b6d4);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: 1.2rem;
            color: #fff;
            position: relative;
            overflow: hidden;
        }

        .logo-icon::after {
            content: '';
            position: absolute;
            inset: 2px;
            border-radius: 8px;
            border: 1px solid rgba(255,255,255,0.2);
        }

        .logo span {
            color: #3b82f6;
        }

        .nav-links {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .nav-links a {
            padding: 10px 18px;
            font-size: 0.9rem;
            font-weight: 500;
            color: #94a3b8;
            border-radius: 8px;
            transition: all 0.3s ease;
            min-height: 44px;
            display: flex;
            align-items: center;
            position: relative;
        }

        .nav-links a:hover {
            color: #ffffff;
            background: rgba(59, 130, 246, 0.1);
        }

        .nav-links a.active {
            color: #3b82f6;
        }

        .nav-cta {
            margin-left: 16px;
            padding: 12px 28px !important;
            background: linear-gradient(135deg, #3b82f6, #2563eb) !important;
            color: #ffffff !important;
            border-radius: 10px !important;
            font-weight: 600 !important;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
            transition: all 0.3s ease !important;
        }

        .nav-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4) !important;
            background: linear-gradient(135deg, #2563eb, #1d4ed8) !important;
        }

        /* Mobile Menu */
        .mobile-toggle {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            padding: 10px;
            min-height: 44px;
            min-width: 44px;
            align-items: center;
            justify-content: center;
            border: none;
            background: none;
            z-index: 1001;
        }

        .mobile-toggle span {
            width: 24px;
            height: 2px;
            background: #e0e6ed;
            border-radius: 2px;
            transition: all 0.3s ease;
        }

        .mobile-toggle.active span:nth-child(1) {
            transform: rotate(45deg) translate(5px, 5px);
        }
        .mobile-toggle.active span:nth-child(2) {
            opacity: 0;
        }
        .mobile-toggle.active span:nth-child(3) {
            transform: rotate(-45deg) translate(5px, -5px);
        }

        /* ===== HERO SECTION ===== */
        .hero {
            position: relative;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding-top: 72px;
            overflow: hidden;
            background: #0a0e17;
        }

        .hero-bg {
            position: absolute;
            inset: 0;
            z-index: 0;
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            inset: 0;
            background:
                radial-gradient(ellipse 80% 60% at 20% 50%, rgba(59, 130, 246, 0.12) 0%, transparent 70%),
                radial-gradient(ellipse 60% 50% at 80% 30%, rgba(6, 182, 212, 0.08) 0%, transparent 60%),
                radial-gradient(ellipse 40% 40% at 50% 80%, rgba(99, 102, 241, 0.06) 0%, transparent 50%);
        }

        .hero-grid-pattern {
            position: absolute;
            inset: 0;
            background-image:
                linear-gradient(rgba(59, 130, 246, 0.04) 1px, transparent 1px),
                linear-gradient(90deg, rgba(59, 130, 246, 0.04) 1px, transparent 1px);
            background-size: 60px 60px;
            animation: gridMove 20s linear infinite;
        }

        .hero-content {
            position: relative;
            z-index: 2;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
        }

        .hero-text {
            animation: slideInLeft 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.2);
            border-radius: 100px;
            font-size: 0.85rem;
            font-weight: 500;
            color: #60a5fa;
            margin-bottom: 24px;
        }

        .hero-badge-dot {
            width: 8px;
            height: 8px;
            background: #3b82f6;
            border-radius: 50%;
            animation: pulseGlow 2s infinite;
        }

        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            color: #ffffff;
            margin-bottom: 24px;
            letter-spacing: -0.03em;
            line-height: 1.1;
        }

        .hero h1 .highlight {
            background: linear-gradient(135deg, #3b82f6, #06b6d4);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-description {
            font-size: 1.15rem;
            color: #94a3b8;
            margin-bottom: 40px;
            max-width: 520px;
            line-height: 1.7;
        }

        .hero-buttons {
            display: flex;
            gap: 16px;
            flex-wrap: wrap;
            margin-bottom: 48px;
        }

        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 16px 36px;
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: #ffffff;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 12px;
            border: none;
            cursor: pointer;
            min-height: 44px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 20px rgba(59, 130, 246, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(59, 130, 246, 0.45);
        }

        .btn-secondary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 16px 36px;
            background: rgba(255, 255, 255, 0.05);
            color: #e0e6ed;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            cursor: pointer;
            min-height: 44px;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .hero-stats {
`;

